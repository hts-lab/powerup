% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/powerup_preprocess.R
\name{powerup_preprocess}
\alias{powerup_preprocess}
\title{PowerUp preprocessing (platform contract)}
\usage{
powerup_preprocess(
  job_id,
  data_version,
  response_set,
  seed,
  local_input_matrix,
  local_gene_expr,
  local_response,
  out_dir,
  n_features = 2000L,
  targets = NULL,
  sensitive_cutoff = 0.75,
  resistant_cutoff = 0.25
)
}
\arguments{
\item{job_id}{Character. Required job identifier for logging/manifest.}

\item{data_version}{Character. Dataset version tag (e.g. "v1").}

\item{response_set}{Character. "crispr" or "drug" (used for modelKey prefix).}

\item{seed}{Integer-like. Deterministic seed for hashing/splits.}

\item{local_input_matrix}{Path to local user matrix CSV (matrix.csv).}

\item{local_gene_expr}{Path to local gene_expression.csv.}

\item{local_response}{Path to local response CSV (CRISPR_response.csv or drug_response.csv staged as response.csv).}

\item{out_dir}{Path to write preprocess artifacts (e.g. /tmp/powerup/\if{html}{\out{<job>}}/preprocess).}

\item{n_features}{Integer. Number of top-variance genes to select from user matrix (default 2000).}

\item{targets}{Optional character vector. Allowlisted perturbations to include (exact match).}

\item{sensitive_cutoff}{Numeric. Default 0.75.}

\item{resistant_cutoff}{Numeric. Default 0.25.}
}
\value{
Invisibly returns a list describing key outputs.
}
\description{
This function is intended to be called by the PowerUp Cloud Run worker.
It MUST read only local staged inputs and MUST write only under out_dir.
}
