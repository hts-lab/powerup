% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/powerup_platform_api.R
\name{powerup_aggregate_user_predictions}
\alias{powerup_aggregate_user_predictions}
\title{Aggregate user predictions across passing models (local runs)}
\usage{
powerup_aggregate_user_predictions(
  out_aggregates_dir,
  models_dir,
  model_status_path = file.path(out_aggregates_dir, "model_status.csv"),
  output_filename = "user_predictions_long.csv",
  verbose = TRUE
)
}
\arguments{
\item{out_aggregates_dir}{Directory containing model_status.csv and where aggregated output is written.}

\item{models_dir}{Directory containing per-model outputs under models_dir/\if{html}{\out{<modelKey>}}/pred_user.csv.}

\item{model_status_path}{Optional explicit path to model_status.csv.
Defaults to file.path(out_aggregates_dir, "model_status.csv").}

\item{output_filename}{Name of the aggregated CSV to write in out_aggregates_dir.}

\item{verbose}{If TRUE, prints progress + warnings.}
}
\value{
A list with counts and output path. (Invisible)
}
\description{
This is a convenience helper for users running PowerUp locally. It mirrors the
platform FINALIZE aggregation behavior by reading model_status.csv and
combining per-model pred_user.csv into a single long table.
}
\details{
Passing models are defined as:
\itemize{
\item status == "OK"
\item skipped == FALSE
}

The function attempts to resolve pred_user.csv paths robustly:
\enumerate{
\item If predUserPath exists locally as-is, it is used.
\item If predUserPath looks like a platform path (e.g., jobs/\if{html}{\out{<jobId>}}/output/models/\if{html}{\out{<modelKey>}}/pred_user.csv),
it is mapped to file.path(models_dir, \if{html}{\out{<modelKey>}}, "pred_user.csv").
\item Otherwise it falls back to file.path(models_dir, \if{html}{\out{<modelKey>}}, "pred_user.csv").
}

Output:
\itemize{
\item Writes user_predictions_long.csv into out_aggregates_dir (configurable filename)
}

Determinism:
\itemize{
\item Models processed in sorted(modelKey) order.
\item Rows sorted by cell_line within each model before writing.
}
}
